{% extends 'layouts/app.twig' %}

{% block title('Homepage') %}

{% block content %}
    <div class="card">
        <div class="card-header text-uppercase font-weight-bold d-flex justify-content-between">
            <span>Note Taking</span>
            <a href="{{ url('notes') }}"></a>
        </div>
        <div class="card-body">
            <i class="fa fa-spinner fa-spin"></i>
        </div>
    </div>
{% endblock content %}